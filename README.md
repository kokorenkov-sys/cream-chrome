# Крем & Хром

Static MVP website for the Cream & Chrome bakery project.

## GitHub Pages

The production-ready static site is in `docs/`.

Deployment is handled by `.github/workflows/deploy.yml`, which uploads `docs` to GitHub Pages on every push to `main`.
